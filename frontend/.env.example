# Leave empty for local dev (Vite's proxy in vite.config.ts forwards /api to localhost:8080).
# On the Pi, set this to the backend's real address, e.g.:
# VITE_API_BASE_URL=http://raspi.local:8080
VITE_API_BASE_URL=
