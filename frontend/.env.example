# Leave empty for local dev (Vite's proxy in vite.config.ts forwards /api to localhost:8181).
# In production, set this to the backend's own public tunnel address, e.g.:
# VITE_API_BASE_URL=https://api.jmzfinance.com
VITE_API_BASE_URL=
